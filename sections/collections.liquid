{% if section.settings.collections != blank %}
    <section class="c-collections">
        <div class="u-container">
            <div class="c-collections__items"> 
                {% for collection in section.settings.collections %}
                    {% assign delay = forloop.index0 | times: 300 %}
                    <a class="c-collections__items__item" href="{{ collection.url }}" data-aos="fade-up" data-aos-delay="{{ delay }}">
                        <figure>
                            <img src="{{ collection.image | img_url: 'medium' }}" alt="{{ collection.title }}">
                            <figcaption>{{ collection.title }}</figcaption>
                        </figure>
                    </a>
                {% endfor %}
            </div>
        </div>
    </section>
{% endif %}

{% schema %}
{
    "name": "Coleções",
    "settings": [
        {
            "type": "collection_list",
            "id": "collections",
            "label": "Escolha as coleções",
            "limit": 5,
        }
    ],
    "presets": [
        {
            "name": "Coleções"
        }
    ]
}
{% endschema %}